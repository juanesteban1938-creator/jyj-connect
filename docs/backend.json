{
  "entities": {
    "Driver": {
      "title": "Driver",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "nombres": { "type": "string" },
        "apellidos": { "type": "string" },
        "cedula": { "type": "string" },
        "telefono": { "type": "string" },
        "categoriaLicencia": { "type": "string" },
        "vencimientoLicencia": { "type": "string", "format": "date-time" }
      }
    },
    "Vehicle": {
      "title": "Vehicle",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "placa": { "type": "string" },
        "marca": { "type": "string" },
        "linea": { "type": "string" },
        "modelo": { "type": "string" },
        "capacidad": { "type": "number" }
      }
    },
    "Service": {
      "title": "Service",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "consecutivo": { "type": "string" },
        "cliente": { "type": "string" },
        "origen": { "type": "string" },
        "destino": { "type": "string" },
        "fecha": { "type": "string", "format": "date-time" },
        "estado": { "type": "string", "enum": ["Programado", "En Servicio", "Finalizado", "Cancelado"] },
        "valorServicio": { "type": "number" },
        "saldo": { "type": "number" }
      }
    }
  },
  "auth": {
    "providers": ["password"]
  },
  "firestore": {
    "/adminUsers/{userId}": {
      "schema": "AdminRole",
      "description": "Usuarios con privilegios de administrador."
    },
    "/conductores/{id}": {
      "schema": "Driver",
      "description": "Base de datos de conductores de la flota."
    },
    "/vehiculos/{id}": {
      "schema": "Vehicle",
      "description": "Inventario de vehículos de la empresa."
    },
    "/clientes/{id}": {
      "schema": "Customer",
      "description": "Cartera de clientes corporativos e individuales."
    },
    "/services/{id}": {
      "schema": "Service",
      "description": "Registro principal de operaciones de transporte."
    },
    "/ubicaciones_gps/{serviceId}": {
      "schema": "VehicleLocation",
      "description": "Señal GPS en tiempo real transmitida por conductores."
    },
    "/pagos_aplicados/{id}": {
      "schema": "PaymentRecord",
      "description": "Auditoría de recaudos y conciliaciones de Nova."
    }
  }
}