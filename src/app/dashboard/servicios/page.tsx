import { Card, CardHeader, CardTitle } from "@/components/ui/card";

export default function ServiciosPage() {
  return (
    <div>
      <h1 className="mb-4 font-headline text-3xl font-bold">Servicios</h1>
       <Card>
        <CardHeader>
          <CardTitle>Gestión de Servicios</CardTitle>
        </CardHeader>
      </Card>
    </div>
  );
}
